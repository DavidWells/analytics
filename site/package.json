{
  "name": "gatsby-starter-theme-workspace",
  "private": true,
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn workspace main develop",
    "build": "yarn workspace main build",
    "serve": "yarn workspace main serve",
    "deploy": "yarn workspace main deploy",
    "deploydev": "yarn workspace main deploydev",
    "sync": "node sync.js"
  },
  "workspaces": [
    "gatsby-theme-base",
    "gatsby-theme-oss-docs",
    "main"
  ],
  "dependencies": {
    "change-case": "^3.1.0"
  },
  "devDependencies": {
    "markdown-magic": "^1.0.0",
    "fs-extra": "^8.1.0"
  }
}
